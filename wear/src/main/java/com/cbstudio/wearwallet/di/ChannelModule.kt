package com.cbstudio.wearwallet.di

import org.koin.dsl.module

/**
 * Channel DI Module - MAINTENANCE MODE
 * ULTRATHINK Phase 16 - 激進禁用策略
 */
val channelModule = module {
    // MAINTENANCE MODE: All channel services disabled
}