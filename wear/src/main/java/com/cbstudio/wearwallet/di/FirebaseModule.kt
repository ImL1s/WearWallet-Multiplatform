package com.cbstudio.wearwallet.di

import org.koin.dsl.module

/**
 * Firebase DI Module - MAINTENANCE MODE
 * ULTRATHINK Phase 16 - 激進禁用策略
 */
val firebaseModule = module {
    // MAINTENANCE MODE: All Firebase services disabled
}
