package com.cbstudio.wearwallet.di

import org.koin.dsl.module

/**
 * KMP Adapter DI Module - MAINTENANCE MODE
 * ULTRATHINK Phase 16 - 激進禁用策略
 */
val kmpAdapterModule = module {
    // MAINTENANCE MODE: All KMP adapter services disabled
}
